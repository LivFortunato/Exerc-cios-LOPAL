let numAleatorio = prompt("Insira um número:"); // Variável numAleatorio armazena o número escolhido pelo usuário.

if(isNaN(numAleatorio)){
    alert("número inválido!"); 
} // Indica a invalidez de uma entrada.

    if(numAleatorio % 2 == 0){ 
        alert("O número é par!")

    } // Condição que avalia se o número escolhido pelo usuário é par.
    else{

        alert("O número não é par!")
    } // Condição que avalia se o número digitado não é par.
