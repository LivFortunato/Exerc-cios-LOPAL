let aleatorioNum = prompt("Insira um número:"); // Variável aleatorioNum armazena o número escolhido pelo usuário.

if(isNaN(aleatorioNum)){
    alert("número inválido!");
} // Indica a invalidez de uma entrada.

    if(aleatorioNum % 5 == 0){ 
        alert("O número é múltiplo de 5!")

    } // Condição que avalia se o número escolhido pelo usuário é múltiplo de 5.
    else{

        alert("O número não é múltiplo de 5!")
    } // Condição que avalia se o número escolhido pelo usuário é múltiplo de 5.